# Neural Network Decision Boundary Demo

An interactive Python demo using **matplotlib sliders** and a simple **MLP classifier**
to visualize decision boundaries on a synthetic dataset (make_circles).
